public static class GameState
{
    // Static variables live for the entire run of the application
    // false = default state (Book visible)
    // true = kitchen unlocked (Book hidden, items visible)
    public static bool isKitchenUnlocked = false;
}